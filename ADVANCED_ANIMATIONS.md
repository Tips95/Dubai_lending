# 🎭 Продвинутые анимации - Документация

## ✨ Реализованные эффекты

### 1. 🎨 Анимированные Blob'ы (Морфинг градиентов)

**Компонент:** `components/AnimatedBlobs.tsx`

**Что делает:**
- 3 больших градиентных пятна с плавной трансформацией формы
- Морфинг границ (border-radius анимация)
- Следование за курсором мыши (parallax эффект)
- Вращение и масштабирование на 360°

**CSS классы:**
- `.blob` - базовый класс
- `.blob-1`, `.blob-2`, `.blob-3` - позиционирование
- `@keyframes blobMorph` - анимация морфинга

**Параметры:**
- Длительность: 20 секунд
- Задержки: 0s, 7s, 14s
- Blur: 40px
- Opacity: 0.3

### 2. 🔷 Плавающие геометрические фигуры

**Встроено в:** `components/AnimatedBlobs.tsx`

**5 разных фигур:**
1. **Треугольник** - clip-path polygon
2. **Круг** - border-radius: 50%
3. **Ромб** - clip-path polygon + rotate
4. **Квадрат** - rotate 45deg
5. **Пятиугольник** - clip-path polygon

**CSS классы:**
- `.geometric-shapes` - контейнер
- `.shape-1` до `.shape-5` - индивидуальные фигуры
- `@keyframes float` - анимация плавания

**Особенности:**
- Разные длительности (15-22 сек)
- Opacity: 0.15-0.25
- Движение вверх-вниз + вращение
- Уникальные градиенты для каждой

### 3. 🎯 3D Transform эффекты для карточек

**CSS класс:** `.card-3d`

**Применено к:**
- ✅ Карточки преимуществ (BenefitsSection)
- ✅ Карточки заработка (EarningsSection)
- ✅ Карточка тарифа (TariffsSection)
- ✅ Контактные карточки (ContactSection)

**Эффект:**
```css
perspective: 1000px;
transform: rotateY(5deg) rotateX(5deg) scale(1.02);
```

**При hover:**
- Вращение по осям Y и X (5°)
- Увеличение на 2%
- Плавный переход 0.6s

### 4. 💫 Градиентный текст с анимацией

**CSS класс:** `.gradient-text-animated`

**Применено к:**
- Hero заголовок "СТАНЬ УСПЕШНЫМ БРОКЕРОМ!"
- Цены в EarningsSection

**Особенности:**
- Анимированный градиент движется слева направо
- 5 цветовых остановок
- Background-size: 200%
- Длительность: 3 секунды
- Бесконечный loop

**Цвета:**
- #a855f7 (purple)
- #ec4899 (pink)
- #8b5cf6 (violet)

### 5. 🔢 Count-Up анимация для цифр

**Компонент:** `components/CountUpAnimation.tsx`

**Функционал:**
- Плавная анимация чисел от 0 до конечного значения
- Intersection Observer - запускается при скролле
- Easing функция: easeOutQuart
- Настраиваемые параметры

**Параметры:**
```typescript
interface CountUpProps {
  end: number           // Конечное значение
  duration?: number     // Длительность (default: 2000ms)
  prefix?: string       // Префикс (напр. "$")
  suffix?: string       // Суффикс (напр. "+")
  decimals?: number     // Количество знаков после запятой
}
```

**Использование:**
```tsx
<CountUpAnimation 
  end={50000} 
  prefix="$" 
  suffix="+" 
  duration={2500}
/>
```

### 6. 🧲 Магнитный эффект (Magnetic Hover)

**Хук:** `hooks/useMagneticEffect.ts`

**Функционал:**
- Элемент следует за курсором при hover
- Настраиваемая сила притяжения
- Плавное возвращение на место

**Использование:**
```tsx
const magneticRef = useMagneticEffect(0.3) // strength

<button ref={magneticRef}>
  Click me
</button>
```

**Параметр strength:**
- 0.1 - слабое притяжение
- 0.3 - среднее (рекомендуется)
- 0.5+ - сильное

### 7. 📝 Text Reveal анимация

**CSS классы:**
- `.text-reveal` - контейнер
- `.text-reveal span` - каждая буква/слово

**Как работает:**
1. Текст разбивается на span'ы
2. Каждый span появляется снизу вверх
3. Каскадные задержки (0.1-0.3s)
4. Запуск при добавлении класса `.is-visible`

**Пример:**
```tsx
<div className="text-reveal is-visible">
  <span>С</span><span>Т</span><span>А</span><span>Н</span><span>Ь</span>
</div>
```

## 🎬 Комбинированные эффекты

### Карточки с полным набором:
```tsx
<div className="card-3d gradient-border magnetic">
  {/* Содержимое */}
</div>
```

Это даст:
- ✅ 3D трансформацию при hover
- ✅ Анимированную градиентную обводку
- ✅ Магнитный эффект следования за курсором

## ⚙️ Настройки производительности

### Will-change оптимизация:
```css
.blob {
  will-change: transform, border-radius;
}
```

### GPU ускорение:
- `transform: translate3d()` вместо `translate()`
- `filter: blur()` с осторожностью
- `perspective` для 3D эффектов

### Reduced Motion:
Все анимации автоматически отключаются для пользователей с:
```css
@media (prefers-reduced-motion: reduce) {
  /* Все анимации отключены */
}
```

## 📊 Производительность

### Оптимизации:
- ✅ CSS анимации вместо JS (где возможно)
- ✅ RequestAnimationFrame для JS анимаций
- ✅ Debouncing для mousemove событий
- ✅ Intersection Observer вместо scroll listeners
- ✅ will-change для критичных элементов

### Рекомендации:
- Blob'ы: максимум 3-4 на странице
- Геометрические фигуры: 5-8 оптимально
- 3D карточки: без ограничений (легковесные)
- CountUp: используйте с threshold 0.1

## 🎨 Кастомизация

### Изменить цвета blob'ов:
```css
.blob {
  background: linear-gradient(45deg, YOUR_COLOR_1, YOUR_COLOR_2, YOUR_COLOR_3);
}
```

### Изменить скорость анимации:
```css
@keyframes blobMorph {
  /* Измените 20s на нужное значение */
  animation-duration: 15s; 
}
```

### Добавить новые фигуры:
```tsx
<div className="shape shape-6" />
```

```css
.shape-6 {
  /* Ваши стили */
  animation-duration: 19s;
  animation-delay: 15s;
}
```

## 🚀 Будущие улучшения (опционально)

1. **Particle System** - частицы на canvas
2. **Cursor Trail** - след от курсора
3. **Ripple Effect** - волны от клика
4. **Confetti Animation** - конфетти при событиях
5. **Liquid Swipe** - переходы между страницами
6. **SVG Morph** - морфинг SVG путей
7. **Glitch Effect** - глитч эффект для текста
8. **Typewriter** - печатная машинка эффект

---

**Дата создания:** 13 января 2026
**Версия:** 2.0 - Advanced Animations
